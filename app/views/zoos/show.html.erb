<h1><%= @zoo.name %></h1>
    <h4>Animal Capacity: <%= @zoo.animal_capacity %></h4>
    <p>Animals Hosted</p>
        <ul>
            <% @zoo.animals.each do |animal| %>
                <li><%= link_to "#{animal.name}", animal_path(animal) %> </li>
            <% end %>
            <%= link_to "Add an Animal", new_animal_path %>
        </ul><br>
    <h4>Zoo Keeper Capacity: <%= @zoo.keeper_capacity %></h4>
    <p>Zoo Keepers Employed</p>
        <ul>
            <% @zoo.keepers.each do |keeper| %>
                <li><%= keeper.name %></li>
            <% end %>
            <%= link_to "Add a Zoo Keeper", new_keeper_path %>
        </ul>